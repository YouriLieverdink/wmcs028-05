import networkx as nx
import matplotlib.pyplot as plt

"""
Apply the HITS or PageRank algorithm to the network, and visualize.
"""
def apply_hits(G: nx.Graph) -> int: #TODO what type of output?
    #modify algorithm a bit have only  hub score for editors and authority for pages.
    pass
